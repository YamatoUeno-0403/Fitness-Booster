            <div class="col-md-3 p-4 border rounded h-50 " >
                <table class="table" >
                    <tbody>
                        <i class="far fa-grin-alt"></i>
                        <div>名前：<%= current_customer.name %></div>
                        <tr>
                            
                            <th scope="col">フォロー</th>
                            <th scope="col"></th>
                        </tr>
                        <tr>
                             <th scope="col">フォロワー</th>
                             <th scope="col"></th>
                        </tr>
                    </tbody>
                </table>    
                    <td>
                    <%= form_with model:@post ,local:true do |f| %>
                      <div class="form-group">
                        <%= f.label :supplement %>
                        <%= f.text_field :supplement, class: 'form-control book_title' %>
                      </div>
                      <div class="form-group">
                        <%= f.label :content %>
                        <%= f.text_area :content, class: 'form-control book_body' %>
                      </div>

                      <div class="form-group">
                        <%= f.submit "投稿する", class: 'btn btn-primary' %></div>
                    <% end %>
                    </td>
                
            </div>